{
  "name": "anatomy-ai-instruction-engine",
  "version": "1.0.0",
  "description": "Converts AI text responses into structured visual commands for the Three.js engine",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "pnpm build && node tests/commands.test.cjs"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "ajv": "8.17.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
